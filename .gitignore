# Quarto build output and cache
/_site/
/.quarto/

# Local preview server logs
quarto_preview_stdout.log
quarto_preview_stderr.log

# Claude Code local session state
.claude/

# Design reference / superseded source assets (not used by the site itself;
# the working copy actually referenced by the hero lives in images/)
HOMEPAGE EXAMPLE.png
backgroung image.jpg

**/*.quarto_ipynb
